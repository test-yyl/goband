name = "goband"
main = "src/index.js"
compatibility_date = "2025-03-01"

# Durable Objects 绑定（删除 storage_backend）
[[durable_objects.bindings]]
name = "GAME_ROOM"
class_name = "GameRoom"

# 迁移配置（使用 new_classes，不是 new_sqlite_classes）
[[migrations]]
tag = "v1"
new_classes = ["GameRoom"]
